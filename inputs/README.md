# inputs/

原料层。默认只用 `manual/`；`raw/`、`common/` 由域扩展或个人按需建立，不预建空目录。

- `manual/`：对话口述、截图等丢了就没有的事实，`<日期>-<主题>.md` 命名，进 wiki 前先落这里。
- `raw/`（按需）：域扩展采集的原始数据，只追加不改写。
- `common/`（按需）：长期人工基准（范围、名册、职责）。

首次接入：读根 `README.md` 与 `AGENTS.md`，在 `config/registry.md` 登记你的项目仓库，然后直接开始用——第一条 ingest 会自动建出需要的目录。

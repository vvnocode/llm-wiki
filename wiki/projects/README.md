# projects/：项目分区

每个外部项目一个子目录：`projects/<项目名>/`，内含该项目的 `index.md` 与主题页（排障案例、架构决策、演进记录等）。

- 只收「与该项目绑定、但跨会话仍需要」的结论；只对该项目当下成立的细节留在项目自己的文档里（判据见 `~/.llm-wiki/docs/schemas/分区与共享.md`）。
- 新建项目分区时：建子目录与 `index.md`，并在根 `wiki/index.md` 的项目分区表登记一行。
- 项目仓库的本机路径在 `~/.llm-wiki/config/registry.md` 登记，页面里用登记名引用。

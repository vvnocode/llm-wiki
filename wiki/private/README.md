# private/：私有区

本目录内容被 `.gitignore` 排除（仅本 README 入库），**物理不出本机**：不进远端、不进分享包、不进团队编译。

必须写在这里：对他人的评价、绩效、薪酬、未公开的组织事项，以及任何你不愿出现在远端的内容。判据见 `~/.llm-wiki/docs/schemas/分区与共享.md`。

注意：这里不是凭证保管处——密码、token、私钥、kubeconfig 任何分区都不收。换机器时本目录需自行迁移（它不在 git 里）。

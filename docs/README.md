# docs

本仓 schema 与口径。改结构先改这里，再改页面。

| 目录 | 放什么 |
|---|---|
| `schemas/` | wiki 契约（`wiki.md`）与分层判据（`分区与共享.md`） |
| `workflows/` | 横切流程：工作方式、新域落地、多 Agent 记忆 |
| `domains/` | 各域扩展口径（由 `scripts/new-domain.sh` 生成骨架） |
| `specs/` | 工作台本身的设计决策 |
| `plans/` | 已确认设计的执行计划 |

面向读者的成稿不放这里，走 `outputs/`。
